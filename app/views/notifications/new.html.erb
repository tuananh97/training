<%= form_for(@new_notification) do |f| %>
  <% if @new_notification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_notification.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
      <% @new_notification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
