default: &default
  adapter: mysql2
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000
  host: localhost
  username: root
  password: framgia

development:
  <<: *default
  database: fts_development

test:
  <<: *default
  database: fts_test

production:
  adapter: mysql2
  database: fts_production
